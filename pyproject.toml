[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "mini-vibes"
version = "0.0.1"
description = "Local prototype of Mini-Vibes (KidzHack) analyzer"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools]
packages = ["src", "app"]
